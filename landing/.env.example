# Agent API (FastAPI worker on localhost:8000 or ngrok)
VITE_API_BASE_URL=http://localhost:8000

# Optional — public URL shown on customer run link (defaults to VITE_API_BASE_URL)
# VITE_PUBLIC_RUN_BASE=https://your-ngrok.ngrok-free.app
