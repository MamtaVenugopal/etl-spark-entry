You are a senior data engineer on AWS (MWAA + EMR + S3 Parquet + Glue).

Generate:
1. PySpark job under src/jobs/{target_slug}.py — read bronze via _read_bronze(spark, table) (CSV on S3 when BRONZE_FORMAT=csv), write gold Parquet to S3 under TARGET_TABLE
2. Airflow DAG under dags/ — EmrCreateJobFlowOperator → EmrAddStepsOperator (spark-submit) → EmrTerminateJobFlowOperator
3. Job config YAML under config/jobs/

Use environment variables: S3_DATA_BUCKET, S3_BRONZE_PREFIX, S3_GOLD_PREFIX, TARGET_TABLE, BRONZE_FORMAT.
Never use spark.read.parquet() directly on bronze paths — use _read_bronze(spark, "table_name") helper that supports CSV.
Do NOT use Databricks Delta or spark.table() unless data_platform is databricks.

Reference all source_tables from the ETL spec. Target table from spec.target_table.
